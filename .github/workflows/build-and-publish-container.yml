name: build and publish container

on:
  workflow_call:
    inputs:
      chipyard-ref:
        required: true
        type: string

permissions:
  contents: read
  packages: write

defaults:
  run:
    shell: bash -leo pipefail {0}

jobs:
  build-and-publish-container:
    name: build and publish container
    runs-on: ubuntu-24.04
    env:
      DOCKER_DATA_ROOT: ${{ github.workspace }}/.ci/docker
      FORCE_NON_EC2: 1
    steps:
      - name: Maximize build space
        uses: jimfangx/maximize-build-space@master
        with:
          root-reserve-mb: 512
          swap-size-mb: 1024
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'

      - name: Check out pull request
        uses: actions/checkout@v4

      - name: Move Docker storage to workspace
        run: |
          sudo systemctl stop docker
          sudo mkdir -p "${DOCKER_DATA_ROOT}"
          printf '{ "data-root": "%s" }\n' "${DOCKER_DATA_ROOT}" | sudo tee /etc/docker/daemon.json
          sudo systemctl start docker
          docker info --format 'Docker root: {{ .DockerRootDir }}'

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish container
        env:
          CHIPYARD_REF: ${{ inputs.chipyard-ref }}
        run: |
          image_name="ghcr.io/${GITHUB_REPOSITORY,,}"
          short_sha="${GITHUB_SHA::12}"
          unix_timestamp="$(date +%s)"

          docker build \
            --file dockerfiles/Dockerfile \
            --target base-with-tools \
            --build-arg "CHIPYARD_REPO=${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git" \
            --build-arg "CHIPYARD_REF=${CHIPYARD_REF}" \
            --label "org.chipyard.ci.unix-timestamp=${unix_timestamp}" \
            --tag "${image_name}:pr-${CHIPYARD_REF}-${short_sha}-${unix_timestamp}" \
            .

          docker push "${image_name}:pr-${CHIPYARD_REF}-${short_sha}-${unix_timestamp}"
