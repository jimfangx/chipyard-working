name: Chipyard Pull Request CI

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review
      - labeled
      - unlabeled

permissions:
  actions: write
  contents: read
  packages: write
  pull-requests: read

concurrency:
  group: pr-ci-${{ github.event.pull_request.number }}
  cancel-in-progress: ${{ contains(github.event.pull_request.labels.*.name, 'ci:disable') }}

jobs:
  check-ci-disable:
    name: check-ci-disable
    if: ${{ github.event.pull_request.user.login != 'dependabot[bot]' }}
    uses: ./.github/workflows/check-ci-disable.yml
    with:
      pr_number: ${{ github.event.pull_request.number }}
      pr_head_ref: ${{ github.event.pull_request.head.ref }}
      pr_head_repo_id: ${{ github.event.pull_request.head.repo.id }}
      pr_labels: ${{ toJson(github.event.pull_request.labels.*.name) }}

  build-setup-ubuntu:
    name: build-setup ubuntu
    needs: check-ci-disable
    if: ${{ needs.check-ci-disable.outputs.ci_disabled != 'true' }}
    uses: ./.github/workflows/build-setup-ubuntu.yml

  build-setup-rhel:
    name: build-setup rhel
    needs: check-ci-disable
    if: ${{ needs.check-ci-disable.outputs.ci_disabled != 'true' }}
    uses: ./.github/workflows/build-setup-rhel.yml

  build-and-publish-container:
    name: build and publish container
    needs:
      - build-setup-ubuntu
      - build-setup-rhel
    uses: ./.github/workflows/build-and-publish-container.yml
    with:
      chipyard_ref: ${{ format('{0}', github.event.pull_request.number) }}
    secrets: inherit

  run-generator-tests:
    name: run generator tests
    needs: build-and-publish-container
    uses: ./.github/workflows/run-generator-tests.yml
    with:
      container_image: ${{ needs.build-and-publish-container.outputs.image_ref }}
    secrets: inherit
